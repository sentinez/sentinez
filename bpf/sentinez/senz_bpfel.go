// Code generated by bpf2go; DO NOT EDIT.
//go:build 386 || amd64 || arm || arm64 || loong64 || mips64le || mipsle || ppc64le || riscv64 || wasm

package sentinezbpf

import (
	"bytes"
	_ "embed"
	"fmt"
	"io"
	"structs"

	"github.com/cilium/ebpf"
)

type SenzIpLpmKey struct {
	_         structs.HostLayout
	Prefixlen uint32
	Ip        uint32
}

// LoadSenz returns the embedded CollectionSpec for Senz.
func LoadSenz() (*ebpf.CollectionSpec, error) {
	reader := bytes.NewReader(_SenzBytes)
	spec, err := ebpf.LoadCollectionSpecFromReader(reader)
	if err != nil {
		return nil, fmt.Errorf("can't load Senz: %w", err)
	}

	return spec, err
}

// LoadSenzObjects loads Senz and converts it into a struct.
//
// The following types are suitable as obj argument:
//
//	*SenzObjects
//	*SenzPrograms
//	*SenzMaps
//
// See ebpf.CollectionSpec.LoadAndAssign documentation for details.
func LoadSenzObjects(obj interface{}, opts *ebpf.CollectionOptions) error {
	spec, err := LoadSenz()
	if err != nil {
		return err
	}

	return spec.LoadAndAssign(obj, opts)
}

// SenzSpecs contains maps and programs before they are loaded into the kernel.
//
// It can be passed ebpf.CollectionSpec.Assign.
type SenzSpecs struct {
	SenzProgramSpecs
	SenzMapSpecs
	SenzVariableSpecs
}

// SenzProgramSpecs contains programs before they are loaded into the kernel.
//
// It can be passed ebpf.CollectionSpec.Assign.
type SenzProgramSpecs struct {
	SenzMain *ebpf.ProgramSpec `ebpf:"senz_main"`
}

// SenzMapSpecs contains maps before they are loaded into the kernel.
//
// It can be passed ebpf.CollectionSpec.Assign.
type SenzMapSpecs struct {
	Blocklist   *ebpf.MapSpec `ebpf:"blocklist"`
	IpBandwidth *ebpf.MapSpec `ebpf:"ip_bandwidth"`
}

// SenzVariableSpecs contains global variables before they are loaded into the kernel.
//
// It can be passed ebpf.CollectionSpec.Assign.
type SenzVariableSpecs struct {
}

// SenzObjects contains all objects after they have been loaded into the kernel.
//
// It can be passed to LoadSenzObjects or ebpf.CollectionSpec.LoadAndAssign.
type SenzObjects struct {
	SenzPrograms
	SenzMaps
	SenzVariables
}

func (o *SenzObjects) Close() error {
	return _SenzClose(
		&o.SenzPrograms,
		&o.SenzMaps,
	)
}

// SenzMaps contains all maps after they have been loaded into the kernel.
//
// It can be passed to LoadSenzObjects or ebpf.CollectionSpec.LoadAndAssign.
type SenzMaps struct {
	Blocklist   *ebpf.Map `ebpf:"blocklist"`
	IpBandwidth *ebpf.Map `ebpf:"ip_bandwidth"`
}

func (m *SenzMaps) Close() error {
	return _SenzClose(
		m.Blocklist,
		m.IpBandwidth,
	)
}

// SenzVariables contains all global variables after they have been loaded into the kernel.
//
// It can be passed to LoadSenzObjects or ebpf.CollectionSpec.LoadAndAssign.
type SenzVariables struct {
}

// SenzPrograms contains all programs after they have been loaded into the kernel.
//
// It can be passed to LoadSenzObjects or ebpf.CollectionSpec.LoadAndAssign.
type SenzPrograms struct {
	SenzMain *ebpf.Program `ebpf:"senz_main"`
}

func (p *SenzPrograms) Close() error {
	return _SenzClose(
		p.SenzMain,
	)
}

func _SenzClose(closers ...io.Closer) error {
	for _, closer := range closers {
		if err := closer.Close(); err != nil {
			return err
		}
	}
	return nil
}

// Do not access this directly.
//
//go:embed senz_bpfel.o
var _SenzBytes []byte
