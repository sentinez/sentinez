// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: sentinez/modules/security/v1/shared.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  FieldSource,
  fieldSourceFromJSON,
  fieldSourceToJSON,
  Logic,
  logicFromJSON,
  logicToJSON,
  Operator,
  operatorFromJSON,
  operatorToJSON,
} from "../../../secure/rule/v1/engine";

export const protobufPackage = "sentinez.modules.security.v1";

/** Single rule */
export interface Rule {
  id: string;
  field: FieldSource;
  operator: Operator;
  value: string;
  key: string;
}

/** Rule group item (recursive structure) */
export interface RuleNode {
  rule?: Rule | undefined;
  group?: RuleGroup | undefined;
}

/** Group of rules */
export interface RuleGroup {
  id: string;
  combinator: Logic;
  rules: RuleNode[];
  not: boolean;
}

function createBaseRule(): Rule {
  return { id: "", field: 0, operator: 0, value: "", key: "" };
}

export const Rule: MessageFns<Rule> = {
  encode(message: Rule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.field !== 0) {
      writer.uint32(16).int32(message.field);
    }
    if (message.operator !== 0) {
      writer.uint32(24).int32(message.operator);
    }
    if (message.value !== "") {
      writer.uint32(34).string(message.value);
    }
    if (message.key !== "") {
      writer.uint32(42).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Rule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.field = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.operator = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.value = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Rule {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      field: isSet(object.field) ? fieldSourceFromJSON(object.field) : 0,
      operator: isSet(object.operator) ? operatorFromJSON(object.operator) : 0,
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      key: isSet(object.key) ? globalThis.String(object.key) : "",
    };
  },

  toJSON(message: Rule): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.field !== 0) {
      obj.field = fieldSourceToJSON(message.field);
    }
    if (message.operator !== 0) {
      obj.operator = operatorToJSON(message.operator);
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Rule>, I>>(base?: I): Rule {
    return Rule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Rule>, I>>(object: I): Rule {
    const message = createBaseRule();
    message.id = object.id ?? "";
    message.field = object.field ?? 0;
    message.operator = object.operator ?? 0;
    message.value = object.value ?? "";
    message.key = object.key ?? "";
    return message;
  },
};

function createBaseRuleNode(): RuleNode {
  return { rule: undefined, group: undefined };
}

export const RuleNode: MessageFns<RuleNode> = {
  encode(message: RuleNode, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rule !== undefined) {
      Rule.encode(message.rule, writer.uint32(10).fork()).join();
    }
    if (message.group !== undefined) {
      RuleGroup.encode(message.group, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RuleNode {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRuleNode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rule = Rule.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.group = RuleGroup.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RuleNode {
    return {
      rule: isSet(object.rule) ? Rule.fromJSON(object.rule) : undefined,
      group: isSet(object.group) ? RuleGroup.fromJSON(object.group) : undefined,
    };
  },

  toJSON(message: RuleNode): unknown {
    const obj: any = {};
    if (message.rule !== undefined) {
      obj.rule = Rule.toJSON(message.rule);
    }
    if (message.group !== undefined) {
      obj.group = RuleGroup.toJSON(message.group);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RuleNode>, I>>(base?: I): RuleNode {
    return RuleNode.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RuleNode>, I>>(object: I): RuleNode {
    const message = createBaseRuleNode();
    message.rule = (object.rule !== undefined && object.rule !== null) ? Rule.fromPartial(object.rule) : undefined;
    message.group = (object.group !== undefined && object.group !== null)
      ? RuleGroup.fromPartial(object.group)
      : undefined;
    return message;
  },
};

function createBaseRuleGroup(): RuleGroup {
  return { id: "", combinator: 0, rules: [], not: false };
}

export const RuleGroup: MessageFns<RuleGroup> = {
  encode(message: RuleGroup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.combinator !== 0) {
      writer.uint32(16).int32(message.combinator);
    }
    for (const v of message.rules) {
      RuleNode.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.not !== false) {
      writer.uint32(32).bool(message.not);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RuleGroup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRuleGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.combinator = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.rules.push(RuleNode.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.not = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RuleGroup {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      combinator: isSet(object.combinator) ? logicFromJSON(object.combinator) : 0,
      rules: globalThis.Array.isArray(object?.rules) ? object.rules.map((e: any) => RuleNode.fromJSON(e)) : [],
      not: isSet(object.not) ? globalThis.Boolean(object.not) : false,
    };
  },

  toJSON(message: RuleGroup): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.combinator !== 0) {
      obj.combinator = logicToJSON(message.combinator);
    }
    if (message.rules?.length) {
      obj.rules = message.rules.map((e) => RuleNode.toJSON(e));
    }
    if (message.not !== false) {
      obj.not = message.not;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RuleGroup>, I>>(base?: I): RuleGroup {
    return RuleGroup.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RuleGroup>, I>>(object: I): RuleGroup {
    const message = createBaseRuleGroup();
    message.id = object.id ?? "";
    message.combinator = object.combinator ?? 0;
    message.rules = object.rules?.map((e) => RuleNode.fromPartial(e)) || [];
    message.not = object.not ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
