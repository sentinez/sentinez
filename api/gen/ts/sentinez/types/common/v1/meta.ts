// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: sentinez/types/common/v1/meta.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentinez.types.common.v1";

export interface Pages {
  index: number;
  size: number;
  total: boolean;
}

function createBasePages(): Pages {
  return { index: 0, size: 0, total: false };
}

export const Pages: MessageFns<Pages> = {
  encode(message: Pages, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).int32(message.index);
    }
    if (message.size !== 0) {
      writer.uint32(16).int32(message.size);
    }
    if (message.total !== false) {
      writer.uint32(24).bool(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Pages {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePages();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.size = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.total = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Pages {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      total: isSet(object.total) ? globalThis.Boolean(object.total) : false,
    };
  },

  toJSON(message: Pages): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.total !== false) {
      obj.total = message.total;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Pages>, I>>(base?: I): Pages {
    return Pages.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Pages>, I>>(object: I): Pages {
    const message = createBasePages();
    message.index = object.index ?? 0;
    message.size = object.size ?? 0;
    message.total = object.total ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
