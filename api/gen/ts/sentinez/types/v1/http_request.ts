// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: sentinez/types/v1/http_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentinez.types.v1";

export interface RequestHeader {
  key: string;
  value: string;
}

export interface QueryValues {
  values: string[];
}

export interface RequestEvent {
  reqId: string;
  method: string;
  scheme: string;
  host: string;
  path: string;
  query: string;
  headers: RequestHeader[];
  protocol: string;
  remoteAddress: string;
  respBody: Uint8Array;
  status: number;
  userAgent: string;
  rayId: string;
  asn: number;
  country: string;
  contentType: string;
}

function createBaseRequestHeader(): RequestHeader {
  return { key: "", value: "" };
}

export const RequestHeader: MessageFns<RequestHeader> = {
  encode(message: RequestHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestHeader {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: RequestHeader): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestHeader>, I>>(base?: I): RequestHeader {
    return RequestHeader.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestHeader>, I>>(object: I): RequestHeader {
    const message = createBaseRequestHeader();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseQueryValues(): QueryValues {
  return { values: [] };
}

export const QueryValues: MessageFns<QueryValues> = {
  encode(message: QueryValues, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.values) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryValues {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryValues();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.values.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryValues {
    return {
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: QueryValues): unknown {
    const obj: any = {};
    if (message.values?.length) {
      obj.values = message.values;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryValues>, I>>(base?: I): QueryValues {
    return QueryValues.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryValues>, I>>(object: I): QueryValues {
    const message = createBaseQueryValues();
    message.values = object.values?.map((e) => e) || [];
    return message;
  },
};

function createBaseRequestEvent(): RequestEvent {
  return {
    reqId: "",
    method: "",
    scheme: "",
    host: "",
    path: "",
    query: "",
    headers: [],
    protocol: "",
    remoteAddress: "",
    respBody: new Uint8Array(0),
    status: 0,
    userAgent: "",
    rayId: "",
    asn: 0,
    country: "",
    contentType: "",
  };
}

export const RequestEvent: MessageFns<RequestEvent> = {
  encode(message: RequestEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reqId !== "") {
      writer.uint32(10).string(message.reqId);
    }
    if (message.method !== "") {
      writer.uint32(18).string(message.method);
    }
    if (message.scheme !== "") {
      writer.uint32(26).string(message.scheme);
    }
    if (message.host !== "") {
      writer.uint32(34).string(message.host);
    }
    if (message.path !== "") {
      writer.uint32(42).string(message.path);
    }
    if (message.query !== "") {
      writer.uint32(50).string(message.query);
    }
    for (const v of message.headers) {
      RequestHeader.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.protocol !== "") {
      writer.uint32(74).string(message.protocol);
    }
    if (message.remoteAddress !== "") {
      writer.uint32(82).string(message.remoteAddress);
    }
    if (message.respBody.length !== 0) {
      writer.uint32(66).bytes(message.respBody);
    }
    if (message.status !== 0) {
      writer.uint32(88).int32(message.status);
    }
    if (message.userAgent !== "") {
      writer.uint32(98).string(message.userAgent);
    }
    if (message.rayId !== "") {
      writer.uint32(106).string(message.rayId);
    }
    if (message.asn !== 0) {
      writer.uint32(112).int64(message.asn);
    }
    if (message.country !== "") {
      writer.uint32(122).string(message.country);
    }
    if (message.contentType !== "") {
      writer.uint32(130).string(message.contentType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reqId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scheme = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.headers.push(RequestHeader.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.protocol = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.remoteAddress = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.respBody = reader.bytes();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.status = reader.int32();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.userAgent = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.rayId = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.asn = longToNumber(reader.int64());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.country = reader.string();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.contentType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestEvent {
    return {
      reqId: isSet(object.reqId) ? globalThis.String(object.reqId) : "",
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      scheme: isSet(object.scheme) ? globalThis.String(object.scheme) : "",
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      headers: globalThis.Array.isArray(object?.headers)
        ? object.headers.map((e: any) => RequestHeader.fromJSON(e))
        : [],
      protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : "",
      remoteAddress: isSet(object.remoteAddress) ? globalThis.String(object.remoteAddress) : "",
      respBody: isSet(object.respBody) ? bytesFromBase64(object.respBody) : new Uint8Array(0),
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      userAgent: isSet(object.userAgent) ? globalThis.String(object.userAgent) : "",
      rayId: isSet(object.rayId) ? globalThis.String(object.rayId) : "",
      asn: isSet(object.asn) ? globalThis.Number(object.asn) : 0,
      country: isSet(object.country) ? globalThis.String(object.country) : "",
      contentType: isSet(object.contentType) ? globalThis.String(object.contentType) : "",
    };
  },

  toJSON(message: RequestEvent): unknown {
    const obj: any = {};
    if (message.reqId !== "") {
      obj.reqId = message.reqId;
    }
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.scheme !== "") {
      obj.scheme = message.scheme;
    }
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.headers?.length) {
      obj.headers = message.headers.map((e) => RequestHeader.toJSON(e));
    }
    if (message.protocol !== "") {
      obj.protocol = message.protocol;
    }
    if (message.remoteAddress !== "") {
      obj.remoteAddress = message.remoteAddress;
    }
    if (message.respBody.length !== 0) {
      obj.respBody = base64FromBytes(message.respBody);
    }
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.userAgent !== "") {
      obj.userAgent = message.userAgent;
    }
    if (message.rayId !== "") {
      obj.rayId = message.rayId;
    }
    if (message.asn !== 0) {
      obj.asn = Math.round(message.asn);
    }
    if (message.country !== "") {
      obj.country = message.country;
    }
    if (message.contentType !== "") {
      obj.contentType = message.contentType;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestEvent>, I>>(base?: I): RequestEvent {
    return RequestEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestEvent>, I>>(object: I): RequestEvent {
    const message = createBaseRequestEvent();
    message.reqId = object.reqId ?? "";
    message.method = object.method ?? "";
    message.scheme = object.scheme ?? "";
    message.host = object.host ?? "";
    message.path = object.path ?? "";
    message.query = object.query ?? "";
    message.headers = object.headers?.map((e) => RequestHeader.fromPartial(e)) || [];
    message.protocol = object.protocol ?? "";
    message.remoteAddress = object.remoteAddress ?? "";
    message.respBody = object.respBody ?? new Uint8Array(0);
    message.status = object.status ?? 0;
    message.userAgent = object.userAgent ?? "";
    message.rayId = object.rayId ?? "";
    message.asn = object.asn ?? 0;
    message.country = object.country ?? "";
    message.contentType = object.contentType ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
