// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: sentinez/types/v1/http.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "sentinez.types.v1";

export interface RequestHeader {
  key: Uint8Array;
  values: Uint8Array[];
}

export interface RequestQuery {
  key: Uint8Array;
  values: Uint8Array[];
}

export interface Request {
  id: string;
  method: string;
  scheme: string;
  host: string;
  path: Uint8Array;
  uri: Uint8Array;
  queries: RequestQuery[];
  headers: RequestHeader[];
  body: Uint8Array;
  protocol: string;
  remoteAddress: string;
  status: number;
  userAgent: Uint8Array;
  rayId: string;
  asn: number;
  country: string;
  contentType: Uint8Array;
  timestamp?: Date | undefined;
  fingerprint: string;
  clientIp: string;
}

export interface HeaderValue {
  values: string[];
}

export interface QueryValue {
  values: string[];
}

export interface RequestEvent {
  id: string;
  method: string;
  scheme: string;
  host: string;
  path: string;
  queries: { [key: string]: QueryValue };
  headers: { [key: string]: HeaderValue };
  protocol: string;
  remoteAddress: string;
  status: number;
  userAgent: string;
  rayId: string;
  asn: number;
  country: string;
  contentType: string;
  timestamp: number;
  fingerprint: string;
  clientIp: string;
}

export interface RequestEvent_QueriesEntry {
  key: string;
  value?: QueryValue | undefined;
}

export interface RequestEvent_HeadersEntry {
  key: string;
  value?: HeaderValue | undefined;
}

function createBaseRequestHeader(): RequestHeader {
  return { key: new Uint8Array(0), values: [] };
}

export const RequestHeader: MessageFns<RequestHeader> = {
  encode(message: RequestHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key.length !== 0) {
      writer.uint32(10).bytes(message.key);
    }
    for (const v of message.values) {
      writer.uint32(18).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.values.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestHeader {
    return {
      key: isSet(object.key) ? bytesFromBase64(object.key) : new Uint8Array(0),
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => bytesFromBase64(e)) : [],
    };
  },

  toJSON(message: RequestHeader): unknown {
    const obj: any = {};
    if (message.key.length !== 0) {
      obj.key = base64FromBytes(message.key);
    }
    if (message.values?.length) {
      obj.values = message.values.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestHeader>, I>>(base?: I): RequestHeader {
    return RequestHeader.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestHeader>, I>>(object: I): RequestHeader {
    const message = createBaseRequestHeader();
    message.key = object.key ?? new Uint8Array(0);
    message.values = object.values?.map((e) => e) || [];
    return message;
  },
};

function createBaseRequestQuery(): RequestQuery {
  return { key: new Uint8Array(0), values: [] };
}

export const RequestQuery: MessageFns<RequestQuery> = {
  encode(message: RequestQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key.length !== 0) {
      writer.uint32(10).bytes(message.key);
    }
    for (const v of message.values) {
      writer.uint32(18).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.values.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestQuery {
    return {
      key: isSet(object.key) ? bytesFromBase64(object.key) : new Uint8Array(0),
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => bytesFromBase64(e)) : [],
    };
  },

  toJSON(message: RequestQuery): unknown {
    const obj: any = {};
    if (message.key.length !== 0) {
      obj.key = base64FromBytes(message.key);
    }
    if (message.values?.length) {
      obj.values = message.values.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestQuery>, I>>(base?: I): RequestQuery {
    return RequestQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestQuery>, I>>(object: I): RequestQuery {
    const message = createBaseRequestQuery();
    message.key = object.key ?? new Uint8Array(0);
    message.values = object.values?.map((e) => e) || [];
    return message;
  },
};

function createBaseRequest(): Request {
  return {
    id: "",
    method: "",
    scheme: "",
    host: "",
    path: new Uint8Array(0),
    uri: new Uint8Array(0),
    queries: [],
    headers: [],
    body: new Uint8Array(0),
    protocol: "",
    remoteAddress: "",
    status: 0,
    userAgent: new Uint8Array(0),
    rayId: "",
    asn: 0,
    country: "",
    contentType: new Uint8Array(0),
    timestamp: undefined,
    fingerprint: "",
    clientIp: "",
  };
}

export const Request: MessageFns<Request> = {
  encode(message: Request, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.method !== "") {
      writer.uint32(18).string(message.method);
    }
    if (message.scheme !== "") {
      writer.uint32(26).string(message.scheme);
    }
    if (message.host !== "") {
      writer.uint32(34).string(message.host);
    }
    if (message.path.length !== 0) {
      writer.uint32(42).bytes(message.path);
    }
    if (message.uri.length !== 0) {
      writer.uint32(50).bytes(message.uri);
    }
    for (const v of message.queries) {
      RequestQuery.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.headers) {
      RequestHeader.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.body.length !== 0) {
      writer.uint32(74).bytes(message.body);
    }
    if (message.protocol !== "") {
      writer.uint32(82).string(message.protocol);
    }
    if (message.remoteAddress !== "") {
      writer.uint32(90).string(message.remoteAddress);
    }
    if (message.status !== 0) {
      writer.uint32(96).int32(message.status);
    }
    if (message.userAgent.length !== 0) {
      writer.uint32(106).bytes(message.userAgent);
    }
    if (message.rayId !== "") {
      writer.uint32(114).string(message.rayId);
    }
    if (message.asn !== 0) {
      writer.uint32(120).int64(message.asn);
    }
    if (message.country !== "") {
      writer.uint32(130).string(message.country);
    }
    if (message.contentType.length !== 0) {
      writer.uint32(138).bytes(message.contentType);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(146).fork()).join();
    }
    if (message.fingerprint !== "") {
      writer.uint32(154).string(message.fingerprint);
    }
    if (message.clientIp !== "") {
      writer.uint32(162).string(message.clientIp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Request {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scheme = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.path = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.uri = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.queries.push(RequestQuery.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.headers.push(RequestHeader.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.body = reader.bytes();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.protocol = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.remoteAddress = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.status = reader.int32();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.userAgent = reader.bytes();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.rayId = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.asn = longToNumber(reader.int64());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.country = reader.string();
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.contentType = reader.bytes();
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.fingerprint = reader.string();
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.clientIp = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Request {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      scheme: isSet(object.scheme) ? globalThis.String(object.scheme) : "",
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      path: isSet(object.path) ? bytesFromBase64(object.path) : new Uint8Array(0),
      uri: isSet(object.uri) ? bytesFromBase64(object.uri) : new Uint8Array(0),
      queries: globalThis.Array.isArray(object?.queries)
        ? object.queries.map((e: any) => RequestQuery.fromJSON(e))
        : [],
      headers: globalThis.Array.isArray(object?.headers)
        ? object.headers.map((e: any) => RequestHeader.fromJSON(e))
        : [],
      body: isSet(object.body) ? bytesFromBase64(object.body) : new Uint8Array(0),
      protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : "",
      remoteAddress: isSet(object.remoteAddress) ? globalThis.String(object.remoteAddress) : "",
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      userAgent: isSet(object.userAgent) ? bytesFromBase64(object.userAgent) : new Uint8Array(0),
      rayId: isSet(object.rayId) ? globalThis.String(object.rayId) : "",
      asn: isSet(object.asn) ? globalThis.Number(object.asn) : 0,
      country: isSet(object.country) ? globalThis.String(object.country) : "",
      contentType: isSet(object.contentType) ? bytesFromBase64(object.contentType) : new Uint8Array(0),
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      fingerprint: isSet(object.fingerprint) ? globalThis.String(object.fingerprint) : "",
      clientIp: isSet(object.clientIp) ? globalThis.String(object.clientIp) : "",
    };
  },

  toJSON(message: Request): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.scheme !== "") {
      obj.scheme = message.scheme;
    }
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.path.length !== 0) {
      obj.path = base64FromBytes(message.path);
    }
    if (message.uri.length !== 0) {
      obj.uri = base64FromBytes(message.uri);
    }
    if (message.queries?.length) {
      obj.queries = message.queries.map((e) => RequestQuery.toJSON(e));
    }
    if (message.headers?.length) {
      obj.headers = message.headers.map((e) => RequestHeader.toJSON(e));
    }
    if (message.body.length !== 0) {
      obj.body = base64FromBytes(message.body);
    }
    if (message.protocol !== "") {
      obj.protocol = message.protocol;
    }
    if (message.remoteAddress !== "") {
      obj.remoteAddress = message.remoteAddress;
    }
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.userAgent.length !== 0) {
      obj.userAgent = base64FromBytes(message.userAgent);
    }
    if (message.rayId !== "") {
      obj.rayId = message.rayId;
    }
    if (message.asn !== 0) {
      obj.asn = Math.round(message.asn);
    }
    if (message.country !== "") {
      obj.country = message.country;
    }
    if (message.contentType.length !== 0) {
      obj.contentType = base64FromBytes(message.contentType);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.fingerprint !== "") {
      obj.fingerprint = message.fingerprint;
    }
    if (message.clientIp !== "") {
      obj.clientIp = message.clientIp;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Request>, I>>(base?: I): Request {
    return Request.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Request>, I>>(object: I): Request {
    const message = createBaseRequest();
    message.id = object.id ?? "";
    message.method = object.method ?? "";
    message.scheme = object.scheme ?? "";
    message.host = object.host ?? "";
    message.path = object.path ?? new Uint8Array(0);
    message.uri = object.uri ?? new Uint8Array(0);
    message.queries = object.queries?.map((e) => RequestQuery.fromPartial(e)) || [];
    message.headers = object.headers?.map((e) => RequestHeader.fromPartial(e)) || [];
    message.body = object.body ?? new Uint8Array(0);
    message.protocol = object.protocol ?? "";
    message.remoteAddress = object.remoteAddress ?? "";
    message.status = object.status ?? 0;
    message.userAgent = object.userAgent ?? new Uint8Array(0);
    message.rayId = object.rayId ?? "";
    message.asn = object.asn ?? 0;
    message.country = object.country ?? "";
    message.contentType = object.contentType ?? new Uint8Array(0);
    message.timestamp = object.timestamp ?? undefined;
    message.fingerprint = object.fingerprint ?? "";
    message.clientIp = object.clientIp ?? "";
    return message;
  },
};

function createBaseHeaderValue(): HeaderValue {
  return { values: [] };
}

export const HeaderValue: MessageFns<HeaderValue> = {
  encode(message: HeaderValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.values) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HeaderValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeaderValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.values.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HeaderValue {
    return {
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: HeaderValue): unknown {
    const obj: any = {};
    if (message.values?.length) {
      obj.values = message.values;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HeaderValue>, I>>(base?: I): HeaderValue {
    return HeaderValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HeaderValue>, I>>(object: I): HeaderValue {
    const message = createBaseHeaderValue();
    message.values = object.values?.map((e) => e) || [];
    return message;
  },
};

function createBaseQueryValue(): QueryValue {
  return { values: [] };
}

export const QueryValue: MessageFns<QueryValue> = {
  encode(message: QueryValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.values) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.values.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryValue {
    return {
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: QueryValue): unknown {
    const obj: any = {};
    if (message.values?.length) {
      obj.values = message.values;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryValue>, I>>(base?: I): QueryValue {
    return QueryValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryValue>, I>>(object: I): QueryValue {
    const message = createBaseQueryValue();
    message.values = object.values?.map((e) => e) || [];
    return message;
  },
};

function createBaseRequestEvent(): RequestEvent {
  return {
    id: "",
    method: "",
    scheme: "",
    host: "",
    path: "",
    queries: {},
    headers: {},
    protocol: "",
    remoteAddress: "",
    status: 0,
    userAgent: "",
    rayId: "",
    asn: 0,
    country: "",
    contentType: "",
    timestamp: 0,
    fingerprint: "",
    clientIp: "",
  };
}

export const RequestEvent: MessageFns<RequestEvent> = {
  encode(message: RequestEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.method !== "") {
      writer.uint32(18).string(message.method);
    }
    if (message.scheme !== "") {
      writer.uint32(26).string(message.scheme);
    }
    if (message.host !== "") {
      writer.uint32(34).string(message.host);
    }
    if (message.path !== "") {
      writer.uint32(42).string(message.path);
    }
    Object.entries(message.queries).forEach(([key, value]) => {
      RequestEvent_QueriesEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    Object.entries(message.headers).forEach(([key, value]) => {
      RequestEvent_HeadersEntry.encode({ key: key as any, value }, writer.uint32(58).fork()).join();
    });
    if (message.protocol !== "") {
      writer.uint32(74).string(message.protocol);
    }
    if (message.remoteAddress !== "") {
      writer.uint32(82).string(message.remoteAddress);
    }
    if (message.status !== 0) {
      writer.uint32(88).int32(message.status);
    }
    if (message.userAgent !== "") {
      writer.uint32(98).string(message.userAgent);
    }
    if (message.rayId !== "") {
      writer.uint32(106).string(message.rayId);
    }
    if (message.asn !== 0) {
      writer.uint32(112).int64(message.asn);
    }
    if (message.country !== "") {
      writer.uint32(122).string(message.country);
    }
    if (message.contentType !== "") {
      writer.uint32(130).string(message.contentType);
    }
    if (message.timestamp !== 0) {
      writer.uint32(136).int64(message.timestamp);
    }
    if (message.fingerprint !== "") {
      writer.uint32(146).string(message.fingerprint);
    }
    if (message.clientIp !== "") {
      writer.uint32(154).string(message.clientIp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scheme = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = RequestEvent_QueriesEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.queries[entry6.key] = entry6.value;
          }
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const entry7 = RequestEvent_HeadersEntry.decode(reader, reader.uint32());
          if (entry7.value !== undefined) {
            message.headers[entry7.key] = entry7.value;
          }
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.protocol = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.remoteAddress = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.status = reader.int32();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.userAgent = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.rayId = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.asn = longToNumber(reader.int64());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.country = reader.string();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.contentType = reader.string();
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.timestamp = longToNumber(reader.int64());
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.fingerprint = reader.string();
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.clientIp = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestEvent {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      method: isSet(object.method) ? globalThis.String(object.method) : "",
      scheme: isSet(object.scheme) ? globalThis.String(object.scheme) : "",
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      queries: isObject(object.queries)
        ? Object.entries(object.queries).reduce<{ [key: string]: QueryValue }>((acc, [key, value]) => {
          acc[key] = QueryValue.fromJSON(value);
          return acc;
        }, {})
        : {},
      headers: isObject(object.headers)
        ? Object.entries(object.headers).reduce<{ [key: string]: HeaderValue }>((acc, [key, value]) => {
          acc[key] = HeaderValue.fromJSON(value);
          return acc;
        }, {})
        : {},
      protocol: isSet(object.protocol) ? globalThis.String(object.protocol) : "",
      remoteAddress: isSet(object.remoteAddress) ? globalThis.String(object.remoteAddress) : "",
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      userAgent: isSet(object.userAgent) ? globalThis.String(object.userAgent) : "",
      rayId: isSet(object.rayId) ? globalThis.String(object.rayId) : "",
      asn: isSet(object.asn) ? globalThis.Number(object.asn) : 0,
      country: isSet(object.country) ? globalThis.String(object.country) : "",
      contentType: isSet(object.contentType) ? globalThis.String(object.contentType) : "",
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
      fingerprint: isSet(object.fingerprint) ? globalThis.String(object.fingerprint) : "",
      clientIp: isSet(object.clientIp) ? globalThis.String(object.clientIp) : "",
    };
  },

  toJSON(message: RequestEvent): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.method !== "") {
      obj.method = message.method;
    }
    if (message.scheme !== "") {
      obj.scheme = message.scheme;
    }
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.queries) {
      const entries = Object.entries(message.queries);
      if (entries.length > 0) {
        obj.queries = {};
        entries.forEach(([k, v]) => {
          obj.queries[k] = QueryValue.toJSON(v);
        });
      }
    }
    if (message.headers) {
      const entries = Object.entries(message.headers);
      if (entries.length > 0) {
        obj.headers = {};
        entries.forEach(([k, v]) => {
          obj.headers[k] = HeaderValue.toJSON(v);
        });
      }
    }
    if (message.protocol !== "") {
      obj.protocol = message.protocol;
    }
    if (message.remoteAddress !== "") {
      obj.remoteAddress = message.remoteAddress;
    }
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.userAgent !== "") {
      obj.userAgent = message.userAgent;
    }
    if (message.rayId !== "") {
      obj.rayId = message.rayId;
    }
    if (message.asn !== 0) {
      obj.asn = Math.round(message.asn);
    }
    if (message.country !== "") {
      obj.country = message.country;
    }
    if (message.contentType !== "") {
      obj.contentType = message.contentType;
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    if (message.fingerprint !== "") {
      obj.fingerprint = message.fingerprint;
    }
    if (message.clientIp !== "") {
      obj.clientIp = message.clientIp;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestEvent>, I>>(base?: I): RequestEvent {
    return RequestEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestEvent>, I>>(object: I): RequestEvent {
    const message = createBaseRequestEvent();
    message.id = object.id ?? "";
    message.method = object.method ?? "";
    message.scheme = object.scheme ?? "";
    message.host = object.host ?? "";
    message.path = object.path ?? "";
    message.queries = Object.entries(object.queries ?? {}).reduce<{ [key: string]: QueryValue }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = QueryValue.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.headers = Object.entries(object.headers ?? {}).reduce<{ [key: string]: HeaderValue }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = HeaderValue.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    message.protocol = object.protocol ?? "";
    message.remoteAddress = object.remoteAddress ?? "";
    message.status = object.status ?? 0;
    message.userAgent = object.userAgent ?? "";
    message.rayId = object.rayId ?? "";
    message.asn = object.asn ?? 0;
    message.country = object.country ?? "";
    message.contentType = object.contentType ?? "";
    message.timestamp = object.timestamp ?? 0;
    message.fingerprint = object.fingerprint ?? "";
    message.clientIp = object.clientIp ?? "";
    return message;
  },
};

function createBaseRequestEvent_QueriesEntry(): RequestEvent_QueriesEntry {
  return { key: "", value: undefined };
}

export const RequestEvent_QueriesEntry: MessageFns<RequestEvent_QueriesEntry> = {
  encode(message: RequestEvent_QueriesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      QueryValue.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestEvent_QueriesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestEvent_QueriesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = QueryValue.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestEvent_QueriesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? QueryValue.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: RequestEvent_QueriesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = QueryValue.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestEvent_QueriesEntry>, I>>(base?: I): RequestEvent_QueriesEntry {
    return RequestEvent_QueriesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestEvent_QueriesEntry>, I>>(object: I): RequestEvent_QueriesEntry {
    const message = createBaseRequestEvent_QueriesEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? QueryValue.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseRequestEvent_HeadersEntry(): RequestEvent_HeadersEntry {
  return { key: "", value: undefined };
}

export const RequestEvent_HeadersEntry: MessageFns<RequestEvent_HeadersEntry> = {
  encode(message: RequestEvent_HeadersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      HeaderValue.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestEvent_HeadersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestEvent_HeadersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = HeaderValue.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestEvent_HeadersEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? HeaderValue.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: RequestEvent_HeadersEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = HeaderValue.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestEvent_HeadersEntry>, I>>(base?: I): RequestEvent_HeadersEntry {
    return RequestEvent_HeadersEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestEvent_HeadersEntry>, I>>(object: I): RequestEvent_HeadersEntry {
    const message = createBaseRequestEvent_HeadersEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? HeaderValue.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
