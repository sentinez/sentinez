// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: sentinez/modules/tenant/v1/tenant.proto
/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Setting } from "../../../dmz/edge/v1/setting";
import { planFromJSON, planToJSON, statusFromJSON, statusToJSON } from "../../../types/v1/known";
import { Pages } from "../../../types/v1/model";
import { Resource } from "./model";
export const protobufPackage = "sentinez.modules.tenant.v1";
function createBaseGetResourceRequest() {
    return { id: "", default: false };
}
export const GetResourceRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        if (message.default !== false) {
            writer.uint32(16).bool(message.default);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetResourceRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.default = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            id: isSet(object.id) ? globalThis.String(object.id) : "",
            default: isSet(object.default) ? globalThis.Boolean(object.default) : false,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        if (message.default !== false) {
            obj.default = message.default;
        }
        return obj;
    },
    create(base) {
        return GetResourceRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetResourceRequest();
        message.id = object.id ?? "";
        message.default = object.default ?? false;
        return message;
    },
};
function createBaseGetResourceByDomainRequest() {
    return { resourceDomain: "" };
}
export const GetResourceByDomainRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.resourceDomain !== "") {
            writer.uint32(10).string(message.resourceDomain);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetResourceByDomainRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.resourceDomain = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { resourceDomain: isSet(object.resourceDomain) ? globalThis.String(object.resourceDomain) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.resourceDomain !== "") {
            obj.resourceDomain = message.resourceDomain;
        }
        return obj;
    },
    create(base) {
        return GetResourceByDomainRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetResourceByDomainRequest();
        message.resourceDomain = object.resourceDomain ?? "";
        return message;
    },
};
function createBaseGetResourceByDomainResponse() {
    return { resource: undefined };
}
export const GetResourceByDomainResponse = {
    encode(message, writer = new BinaryWriter()) {
        if (message.resource !== undefined) {
            Resource.encode(message.resource, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetResourceByDomainResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.resource = Resource.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { resource: isSet(object.resource) ? Resource.fromJSON(object.resource) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.resource !== undefined) {
            obj.resource = Resource.toJSON(message.resource);
        }
        return obj;
    },
    create(base) {
        return GetResourceByDomainResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetResourceByDomainResponse();
        message.resource = (object.resource !== undefined && object.resource !== null)
            ? Resource.fromPartial(object.resource)
            : undefined;
        return message;
    },
};
function createBaseGetResourceResponse() {
    return { resource: undefined };
}
export const GetResourceResponse = {
    encode(message, writer = new BinaryWriter()) {
        if (message.resource !== undefined) {
            Resource.encode(message.resource, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetResourceResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.resource = Resource.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { resource: isSet(object.resource) ? Resource.fromJSON(object.resource) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.resource !== undefined) {
            obj.resource = Resource.toJSON(message.resource);
        }
        return obj;
    },
    create(base) {
        return GetResourceResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetResourceResponse();
        message.resource = (object.resource !== undefined && object.resource !== null)
            ? Resource.fromPartial(object.resource)
            : undefined;
        return message;
    },
};
function createBaseDeleteResourceRequest() {
    return { id: "" };
}
export const DeleteResourceRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDeleteResourceRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        return obj;
    },
    create(base) {
        return DeleteResourceRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseDeleteResourceRequest();
        message.id = object.id ?? "";
        return message;
    },
};
function createBaseDeleteResourceResponse() {
    return {};
}
export const DeleteResourceResponse = {
    encode(_, writer = new BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDeleteResourceResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return DeleteResourceResponse.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseDeleteResourceResponse();
        return message;
    },
};
function createBaseUpdateResourceRequest() {
    return { id: "", resourceDomain: "", resourceName: "", status: 0, plan: 0 };
}
export const UpdateResourceRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        if (message.resourceDomain !== "") {
            writer.uint32(18).string(message.resourceDomain);
        }
        if (message.resourceName !== "") {
            writer.uint32(26).string(message.resourceName);
        }
        if (message.status !== 0) {
            writer.uint32(32).int32(message.status);
        }
        if (message.plan !== 0) {
            writer.uint32(40).int32(message.plan);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUpdateResourceRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.resourceDomain = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.resourceName = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.status = reader.int32();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.plan = reader.int32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            id: isSet(object.id) ? globalThis.String(object.id) : "",
            resourceDomain: isSet(object.resourceDomain) ? globalThis.String(object.resourceDomain) : "",
            resourceName: isSet(object.resourceName) ? globalThis.String(object.resourceName) : "",
            status: isSet(object.status) ? statusFromJSON(object.status) : 0,
            plan: isSet(object.plan) ? planFromJSON(object.plan) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        if (message.resourceDomain !== "") {
            obj.resourceDomain = message.resourceDomain;
        }
        if (message.resourceName !== "") {
            obj.resourceName = message.resourceName;
        }
        if (message.status !== 0) {
            obj.status = statusToJSON(message.status);
        }
        if (message.plan !== 0) {
            obj.plan = planToJSON(message.plan);
        }
        return obj;
    },
    create(base) {
        return UpdateResourceRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUpdateResourceRequest();
        message.id = object.id ?? "";
        message.resourceDomain = object.resourceDomain ?? "";
        message.resourceName = object.resourceName ?? "";
        message.status = object.status ?? 0;
        message.plan = object.plan ?? 0;
        return message;
    },
};
function createBaseUpdateResourceResponse() {
    return {};
}
export const UpdateResourceResponse = {
    encode(_, writer = new BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUpdateResourceResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return UpdateResourceResponse.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseUpdateResourceResponse();
        return message;
    },
};
function createBaseCreateResourceRequest() {
    return { resourceSetting: undefined, resourceDomain: "", resourceName: "", status: 0, plan: 0 };
}
export const CreateResourceRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.resourceSetting !== undefined) {
            Setting.encode(message.resourceSetting, writer.uint32(10).fork()).join();
        }
        if (message.resourceDomain !== "") {
            writer.uint32(18).string(message.resourceDomain);
        }
        if (message.resourceName !== "") {
            writer.uint32(26).string(message.resourceName);
        }
        if (message.status !== 0) {
            writer.uint32(32).int32(message.status);
        }
        if (message.plan !== 0) {
            writer.uint32(40).int32(message.plan);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCreateResourceRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.resourceSetting = Setting.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.resourceDomain = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.resourceName = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.status = reader.int32();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.plan = reader.int32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            resourceSetting: isSet(object.resourceSetting) ? Setting.fromJSON(object.resourceSetting) : undefined,
            resourceDomain: isSet(object.resourceDomain) ? globalThis.String(object.resourceDomain) : "",
            resourceName: isSet(object.resourceName) ? globalThis.String(object.resourceName) : "",
            status: isSet(object.status) ? statusFromJSON(object.status) : 0,
            plan: isSet(object.plan) ? planFromJSON(object.plan) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.resourceSetting !== undefined) {
            obj.resourceSetting = Setting.toJSON(message.resourceSetting);
        }
        if (message.resourceDomain !== "") {
            obj.resourceDomain = message.resourceDomain;
        }
        if (message.resourceName !== "") {
            obj.resourceName = message.resourceName;
        }
        if (message.status !== 0) {
            obj.status = statusToJSON(message.status);
        }
        if (message.plan !== 0) {
            obj.plan = planToJSON(message.plan);
        }
        return obj;
    },
    create(base) {
        return CreateResourceRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCreateResourceRequest();
        message.resourceSetting = (object.resourceSetting !== undefined && object.resourceSetting !== null)
            ? Setting.fromPartial(object.resourceSetting)
            : undefined;
        message.resourceDomain = object.resourceDomain ?? "";
        message.resourceName = object.resourceName ?? "";
        message.status = object.status ?? 0;
        message.plan = object.plan ?? 0;
        return message;
    },
};
function createBaseCreateResourceResponse() {
    return { resource: undefined };
}
export const CreateResourceResponse = {
    encode(message, writer = new BinaryWriter()) {
        if (message.resource !== undefined) {
            Resource.encode(message.resource, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCreateResourceResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.resource = Resource.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { resource: isSet(object.resource) ? Resource.fromJSON(object.resource) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.resource !== undefined) {
            obj.resource = Resource.toJSON(message.resource);
        }
        return obj;
    },
    create(base) {
        return CreateResourceResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCreateResourceResponse();
        message.resource = (object.resource !== undefined && object.resource !== null)
            ? Resource.fromPartial(object.resource)
            : undefined;
        return message;
    },
};
function createBaseListResourceRequest() {
    return { page: undefined, status: 0, plan: 0, resourceName: "", resourceDomain: "" };
}
export const ListResourceRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.page !== undefined) {
            Pages.encode(message.page, writer.uint32(10).fork()).join();
        }
        if (message.status !== 0) {
            writer.uint32(16).int32(message.status);
        }
        if (message.plan !== 0) {
            writer.uint32(24).int32(message.plan);
        }
        if (message.resourceName !== "") {
            writer.uint32(34).string(message.resourceName);
        }
        if (message.resourceDomain !== "") {
            writer.uint32(42).string(message.resourceDomain);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListResourceRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.page = Pages.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.status = reader.int32();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.plan = reader.int32();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.resourceName = reader.string();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.resourceDomain = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            page: isSet(object.page) ? Pages.fromJSON(object.page) : undefined,
            status: isSet(object.status) ? statusFromJSON(object.status) : 0,
            plan: isSet(object.plan) ? planFromJSON(object.plan) : 0,
            resourceName: isSet(object.resourceName) ? globalThis.String(object.resourceName) : "",
            resourceDomain: isSet(object.resourceDomain) ? globalThis.String(object.resourceDomain) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.page !== undefined) {
            obj.page = Pages.toJSON(message.page);
        }
        if (message.status !== 0) {
            obj.status = statusToJSON(message.status);
        }
        if (message.plan !== 0) {
            obj.plan = planToJSON(message.plan);
        }
        if (message.resourceName !== "") {
            obj.resourceName = message.resourceName;
        }
        if (message.resourceDomain !== "") {
            obj.resourceDomain = message.resourceDomain;
        }
        return obj;
    },
    create(base) {
        return ListResourceRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListResourceRequest();
        message.page = (object.page !== undefined && object.page !== null) ? Pages.fromPartial(object.page) : undefined;
        message.status = object.status ?? 0;
        message.plan = object.plan ?? 0;
        message.resourceName = object.resourceName ?? "";
        message.resourceDomain = object.resourceDomain ?? "";
        return message;
    },
};
function createBaseListResourceResponse() {
    return { total: 0, resources: [] };
}
export const ListResourceResponse = {
    encode(message, writer = new BinaryWriter()) {
        if (message.total !== 0) {
            writer.uint32(8).int64(message.total);
        }
        for (const v of message.resources) {
            Resource.encode(v, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListResourceResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.total = longToNumber(reader.int64());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.resources.push(Resource.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            total: isSet(object.total) ? globalThis.Number(object.total) : 0,
            resources: globalThis.Array.isArray(object?.resources)
                ? object.resources.map((e) => Resource.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.total !== 0) {
            obj.total = Math.round(message.total);
        }
        if (message.resources?.length) {
            obj.resources = message.resources.map((e) => Resource.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return ListResourceResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListResourceResponse();
        message.total = object.total ?? 0;
        message.resources = object.resources?.map((e) => Resource.fromPartial(e)) || [];
        return message;
    },
};
function createBaseStatusResponse() {
    return { message: "" };
}
export const StatusResponse = {
    encode(message, writer = new BinaryWriter()) {
        if (message.message !== "") {
            writer.uint32(10).string(message.message);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStatusResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.message = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.message !== "") {
            obj.message = message.message;
        }
        return obj;
    },
    create(base) {
        return StatusResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStatusResponse();
        message.message = object.message ?? "";
        return message;
    },
};
function createBaseStatusRequest() {
    return {};
}
export const StatusRequest = {
    encode(_, writer = new BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStatusRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return StatusRequest.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseStatusRequest();
        return message;
    },
};
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
