// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: sentinez/modules/security/v1/security.proto
/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { FieldMask } from "../../../../google/protobuf/field_mask";
import { Pages } from "../../../types/v1/model";
import { RuleBased } from "./model";
export const protobufPackage = "sentinez.modules.security.v1";
function createBaseCreateRuleBasedRequest() {
    return { ruleBased: undefined };
}
export const CreateRuleBasedRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.ruleBased !== undefined) {
            RuleBased.encode(message.ruleBased, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCreateRuleBasedRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.ruleBased = RuleBased.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { ruleBased: isSet(object.ruleBased) ? RuleBased.fromJSON(object.ruleBased) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.ruleBased !== undefined) {
            obj.ruleBased = RuleBased.toJSON(message.ruleBased);
        }
        return obj;
    },
    create(base) {
        return CreateRuleBasedRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCreateRuleBasedRequest();
        message.ruleBased = (object.ruleBased !== undefined && object.ruleBased !== null)
            ? RuleBased.fromPartial(object.ruleBased)
            : undefined;
        return message;
    },
};
function createBaseCreateRuleBasedResponse() {
    return { id: "" };
}
export const CreateRuleBasedResponse = {
    encode(message, writer = new BinaryWriter()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseCreateRuleBasedResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        return obj;
    },
    create(base) {
        return CreateRuleBasedResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseCreateRuleBasedResponse();
        message.id = object.id ?? "";
        return message;
    },
};
function createBaseGetRuleBasedRequest() {
    return { id: "" };
}
export const GetRuleBasedRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetRuleBasedRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        return obj;
    },
    create(base) {
        return GetRuleBasedRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetRuleBasedRequest();
        message.id = object.id ?? "";
        return message;
    },
};
function createBaseGetRuleBasedResponse() {
    return { ruleBased: undefined };
}
export const GetRuleBasedResponse = {
    encode(message, writer = new BinaryWriter()) {
        if (message.ruleBased !== undefined) {
            RuleBased.encode(message.ruleBased, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetRuleBasedResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.ruleBased = RuleBased.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { ruleBased: isSet(object.ruleBased) ? RuleBased.fromJSON(object.ruleBased) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.ruleBased !== undefined) {
            obj.ruleBased = RuleBased.toJSON(message.ruleBased);
        }
        return obj;
    },
    create(base) {
        return GetRuleBasedResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetRuleBasedResponse();
        message.ruleBased = (object.ruleBased !== undefined && object.ruleBased !== null)
            ? RuleBased.fromPartial(object.ruleBased)
            : undefined;
        return message;
    },
};
function createBaseUpdateRuleBasedRequest() {
    return { id: "", ruleBased: undefined, updateMask: undefined };
}
export const UpdateRuleBasedRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        if (message.ruleBased !== undefined) {
            RuleBased.encode(message.ruleBased, writer.uint32(18).fork()).join();
        }
        if (message.updateMask !== undefined) {
            FieldMask.encode(FieldMask.wrap(message.updateMask), writer.uint32(26).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUpdateRuleBasedRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.ruleBased = RuleBased.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.updateMask = FieldMask.unwrap(FieldMask.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            id: isSet(object.id) ? globalThis.String(object.id) : "",
            ruleBased: isSet(object.ruleBased) ? RuleBased.fromJSON(object.ruleBased) : undefined,
            updateMask: isSet(object.updateMask) ? FieldMask.unwrap(FieldMask.fromJSON(object.updateMask)) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        if (message.ruleBased !== undefined) {
            obj.ruleBased = RuleBased.toJSON(message.ruleBased);
        }
        if (message.updateMask !== undefined) {
            obj.updateMask = FieldMask.toJSON(FieldMask.wrap(message.updateMask));
        }
        return obj;
    },
    create(base) {
        return UpdateRuleBasedRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUpdateRuleBasedRequest();
        message.id = object.id ?? "";
        message.ruleBased = (object.ruleBased !== undefined && object.ruleBased !== null)
            ? RuleBased.fromPartial(object.ruleBased)
            : undefined;
        message.updateMask = object.updateMask ?? undefined;
        return message;
    },
};
function createBaseUpdateRuleBasedResponse() {
    return { ruleBased: undefined };
}
export const UpdateRuleBasedResponse = {
    encode(message, writer = new BinaryWriter()) {
        if (message.ruleBased !== undefined) {
            RuleBased.encode(message.ruleBased, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUpdateRuleBasedResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.ruleBased = RuleBased.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { ruleBased: isSet(object.ruleBased) ? RuleBased.fromJSON(object.ruleBased) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.ruleBased !== undefined) {
            obj.ruleBased = RuleBased.toJSON(message.ruleBased);
        }
        return obj;
    },
    create(base) {
        return UpdateRuleBasedResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUpdateRuleBasedResponse();
        message.ruleBased = (object.ruleBased !== undefined && object.ruleBased !== null)
            ? RuleBased.fromPartial(object.ruleBased)
            : undefined;
        return message;
    },
};
function createBaseDeleteRuleBasedRequest() {
    return { id: "" };
}
export const DeleteRuleBasedRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDeleteRuleBasedRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        return obj;
    },
    create(base) {
        return DeleteRuleBasedRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseDeleteRuleBasedRequest();
        message.id = object.id ?? "";
        return message;
    },
};
function createBaseDeleteRuleBasedResponse() {
    return {};
}
export const DeleteRuleBasedResponse = {
    encode(_, writer = new BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseDeleteRuleBasedResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return DeleteRuleBasedResponse.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseDeleteRuleBasedResponse();
        return message;
    },
};
function createBaseListRuleBasedsRequest() {
    return { page: undefined, ids: [] };
}
export const ListRuleBasedsRequest = {
    encode(message, writer = new BinaryWriter()) {
        if (message.page !== undefined) {
            Pages.encode(message.page, writer.uint32(10).fork()).join();
        }
        for (const v of message.ids) {
            writer.uint32(82).string(v);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListRuleBasedsRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.page = Pages.decode(reader, reader.uint32());
                    continue;
                }
                case 10: {
                    if (tag !== 82) {
                        break;
                    }
                    message.ids.push(reader.string());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            page: isSet(object.page) ? Pages.fromJSON(object.page) : undefined,
            ids: globalThis.Array.isArray(object?.ids) ? object.ids.map((e) => globalThis.String(e)) : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.page !== undefined) {
            obj.page = Pages.toJSON(message.page);
        }
        if (message.ids?.length) {
            obj.ids = message.ids;
        }
        return obj;
    },
    create(base) {
        return ListRuleBasedsRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListRuleBasedsRequest();
        message.page = (object.page !== undefined && object.page !== null) ? Pages.fromPartial(object.page) : undefined;
        message.ids = object.ids?.map((e) => e) || [];
        return message;
    },
};
function createBaseListRuleBasedsResponse() {
    return { ruleBaseds: [], total: 0 };
}
export const ListRuleBasedsResponse = {
    encode(message, writer = new BinaryWriter()) {
        for (const v of message.ruleBaseds) {
            RuleBased.encode(v, writer.uint32(10).fork()).join();
        }
        if (message.total !== 0) {
            writer.uint32(16).int64(message.total);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListRuleBasedsResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.ruleBaseds.push(RuleBased.decode(reader, reader.uint32()));
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.total = longToNumber(reader.int64());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            ruleBaseds: globalThis.Array.isArray(object?.ruleBaseds)
                ? object.ruleBaseds.map((e) => RuleBased.fromJSON(e))
                : [],
            total: isSet(object.total) ? globalThis.Number(object.total) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.ruleBaseds?.length) {
            obj.ruleBaseds = message.ruleBaseds.map((e) => RuleBased.toJSON(e));
        }
        if (message.total !== 0) {
            obj.total = Math.round(message.total);
        }
        return obj;
    },
    create(base) {
        return ListRuleBasedsResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListRuleBasedsResponse();
        message.ruleBaseds = object.ruleBaseds?.map((e) => RuleBased.fromPartial(e)) || [];
        message.total = object.total ?? 0;
        return message;
    },
};
function createBaseStatusRequest() {
    return {};
}
export const StatusRequest = {
    encode(_, writer = new BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStatusRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return StatusRequest.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseStatusRequest();
        return message;
    },
};
function createBaseStatusResponse() {
    return { msg: "" };
}
export const StatusResponse = {
    encode(message, writer = new BinaryWriter()) {
        if (message.msg !== "") {
            writer.uint32(10).string(message.msg);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseStatusResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.msg = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { msg: isSet(object.msg) ? globalThis.String(object.msg) : "" };
    },
    toJSON(message) {
        const obj = {};
        if (message.msg !== "") {
            obj.msg = message.msg;
        }
        return obj;
    },
    create(base) {
        return StatusResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseStatusResponse();
        message.msg = object.msg ?? "";
        return message;
    },
};
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
