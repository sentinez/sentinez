// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: sentinez/dmz/edge/v1/setting.proto
/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { CDN } from "../../../cdn/rule/v1/cdn";
import { RuleBased as RuleBased1, RuleBasedLite } from "../../../secure/rule/v1/engine";
export const protobufPackage = "sentinez.dmz.edge.v1";
export var BalanceStrategy;
(function (BalanceStrategy) {
    BalanceStrategy[BalanceStrategy["BALANCE_STRATEGY_UNSPECIFIED"] = 0] = "BALANCE_STRATEGY_UNSPECIFIED";
    BalanceStrategy[BalanceStrategy["BALANCE_STRATEGY_ROUND_ROBIN"] = 1] = "BALANCE_STRATEGY_ROUND_ROBIN";
    BalanceStrategy[BalanceStrategy["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(BalanceStrategy || (BalanceStrategy = {}));
export function balanceStrategyFromJSON(object) {
    switch (object) {
        case 0:
        case "BALANCE_STRATEGY_UNSPECIFIED":
            return BalanceStrategy.BALANCE_STRATEGY_UNSPECIFIED;
        case 1:
        case "BALANCE_STRATEGY_ROUND_ROBIN":
            return BalanceStrategy.BALANCE_STRATEGY_ROUND_ROBIN;
        case -1:
        case "UNRECOGNIZED":
        default:
            return BalanceStrategy.UNRECOGNIZED;
    }
}
export function balanceStrategyToJSON(object) {
    switch (object) {
        case BalanceStrategy.BALANCE_STRATEGY_UNSPECIFIED:
            return "BALANCE_STRATEGY_UNSPECIFIED";
        case BalanceStrategy.BALANCE_STRATEGY_ROUND_ROBIN:
            return "BALANCE_STRATEGY_ROUND_ROBIN";
        case BalanceStrategy.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
export var ProxyProtocol;
(function (ProxyProtocol) {
    ProxyProtocol[ProxyProtocol["PROXY_PROTOCOL_UNSPECIFIED"] = 0] = "PROXY_PROTOCOL_UNSPECIFIED";
    ProxyProtocol[ProxyProtocol["PROXY_PROTOCOL_HTTP"] = 1] = "PROXY_PROTOCOL_HTTP";
    ProxyProtocol[ProxyProtocol["PROXY_PROTOCOL_HTTPS"] = 2] = "PROXY_PROTOCOL_HTTPS";
    ProxyProtocol[ProxyProtocol["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(ProxyProtocol || (ProxyProtocol = {}));
export function proxyProtocolFromJSON(object) {
    switch (object) {
        case 0:
        case "PROXY_PROTOCOL_UNSPECIFIED":
            return ProxyProtocol.PROXY_PROTOCOL_UNSPECIFIED;
        case 1:
        case "PROXY_PROTOCOL_HTTP":
            return ProxyProtocol.PROXY_PROTOCOL_HTTP;
        case 2:
        case "PROXY_PROTOCOL_HTTPS":
            return ProxyProtocol.PROXY_PROTOCOL_HTTPS;
        case -1:
        case "UNRECOGNIZED":
        default:
            return ProxyProtocol.UNRECOGNIZED;
    }
}
export function proxyProtocolToJSON(object) {
    switch (object) {
        case ProxyProtocol.PROXY_PROTOCOL_UNSPECIFIED:
            return "PROXY_PROTOCOL_UNSPECIFIED";
        case ProxyProtocol.PROXY_PROTOCOL_HTTP:
            return "PROXY_PROTOCOL_HTTP";
        case ProxyProtocol.PROXY_PROTOCOL_HTTPS:
            return "PROXY_PROTOCOL_HTTPS";
        case ProxyProtocol.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
function createBaseSetting() {
    return { metadata: undefined, server: undefined, security: undefined, controller: undefined, personal: undefined };
}
export const Setting = {
    encode(message, writer = new BinaryWriter()) {
        if (message.metadata !== undefined) {
            Metadata.encode(message.metadata, writer.uint32(10).fork()).join();
        }
        if (message.server !== undefined) {
            Server.encode(message.server, writer.uint32(18).fork()).join();
        }
        if (message.security !== undefined) {
            Security.encode(message.security, writer.uint32(26).fork()).join();
        }
        if (message.controller !== undefined) {
            Controller.encode(message.controller, writer.uint32(34).fork()).join();
        }
        if (message.personal !== undefined) {
            Personal.encode(message.personal, writer.uint32(42).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSetting();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.metadata = Metadata.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.server = Server.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.security = Security.decode(reader, reader.uint32());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.controller = Controller.decode(reader, reader.uint32());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.personal = Personal.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
            server: isSet(object.server) ? Server.fromJSON(object.server) : undefined,
            security: isSet(object.security) ? Security.fromJSON(object.security) : undefined,
            controller: isSet(object.controller) ? Controller.fromJSON(object.controller) : undefined,
            personal: isSet(object.personal) ? Personal.fromJSON(object.personal) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.metadata !== undefined) {
            obj.metadata = Metadata.toJSON(message.metadata);
        }
        if (message.server !== undefined) {
            obj.server = Server.toJSON(message.server);
        }
        if (message.security !== undefined) {
            obj.security = Security.toJSON(message.security);
        }
        if (message.controller !== undefined) {
            obj.controller = Controller.toJSON(message.controller);
        }
        if (message.personal !== undefined) {
            obj.personal = Personal.toJSON(message.personal);
        }
        return obj;
    },
    create(base) {
        return Setting.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSetting();
        message.metadata = (object.metadata !== undefined && object.metadata !== null)
            ? Metadata.fromPartial(object.metadata)
            : undefined;
        message.server = (object.server !== undefined && object.server !== null)
            ? Server.fromPartial(object.server)
            : undefined;
        message.security = (object.security !== undefined && object.security !== null)
            ? Security.fromPartial(object.security)
            : undefined;
        message.controller = (object.controller !== undefined && object.controller !== null)
            ? Controller.fromPartial(object.controller)
            : undefined;
        message.personal = (object.personal !== undefined && object.personal !== null)
            ? Personal.fromPartial(object.personal)
            : undefined;
        return message;
    },
};
function createBaseMetadata() {
    return {};
}
export const Metadata = {
    encode(_, writer = new BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMetadata();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return Metadata.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBaseMetadata();
        return message;
    },
};
function createBaseServer() {
    return { name: "", locations: [] };
}
export const Server = {
    encode(message, writer = new BinaryWriter()) {
        if (message.name !== "") {
            writer.uint32(10).string(message.name);
        }
        for (const v of message.locations) {
            Location.encode(v, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseServer();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.locations.push(Location.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            name: isSet(object.name) ? globalThis.String(object.name) : "",
            locations: globalThis.Array.isArray(object?.locations)
                ? object.locations.map((e) => Location.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.name !== "") {
            obj.name = message.name;
        }
        if (message.locations?.length) {
            obj.locations = message.locations.map((e) => Location.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return Server.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseServer();
        message.name = object.name ?? "";
        message.locations = object.locations?.map((e) => Location.fromPartial(e)) || [];
        return message;
    },
};
function createBaseLocation() {
    return { location: "", proxyRewrite: "", proxyPass: [], balanceStrategy: 0, proxySetHeaders: {} };
}
export const Location = {
    encode(message, writer = new BinaryWriter()) {
        if (message.location !== "") {
            writer.uint32(10).string(message.location);
        }
        if (message.proxyRewrite !== "") {
            writer.uint32(18).string(message.proxyRewrite);
        }
        for (const v of message.proxyPass) {
            Upstream.encode(v, writer.uint32(26).fork()).join();
        }
        if (message.balanceStrategy !== 0) {
            writer.uint32(32).int32(message.balanceStrategy);
        }
        Object.entries(message.proxySetHeaders).forEach(([key, value]) => {
            Location_ProxySetHeadersEntry.encode({ key: key, value }, writer.uint32(42).fork()).join();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseLocation();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.location = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.proxyRewrite = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.proxyPass.push(Upstream.decode(reader, reader.uint32()));
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.balanceStrategy = reader.int32();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    const entry5 = Location_ProxySetHeadersEntry.decode(reader, reader.uint32());
                    if (entry5.value !== undefined) {
                        message.proxySetHeaders[entry5.key] = entry5.value;
                    }
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            location: isSet(object.location) ? globalThis.String(object.location) : "",
            proxyRewrite: isSet(object.proxyRewrite) ? globalThis.String(object.proxyRewrite) : "",
            proxyPass: globalThis.Array.isArray(object?.proxyPass)
                ? object.proxyPass.map((e) => Upstream.fromJSON(e))
                : [],
            balanceStrategy: isSet(object.balanceStrategy) ? balanceStrategyFromJSON(object.balanceStrategy) : 0,
            proxySetHeaders: isObject(object.proxySetHeaders)
                ? Object.entries(object.proxySetHeaders).reduce((acc, [key, value]) => {
                    acc[key] = String(value);
                    return acc;
                }, {})
                : {},
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.location !== "") {
            obj.location = message.location;
        }
        if (message.proxyRewrite !== "") {
            obj.proxyRewrite = message.proxyRewrite;
        }
        if (message.proxyPass?.length) {
            obj.proxyPass = message.proxyPass.map((e) => Upstream.toJSON(e));
        }
        if (message.balanceStrategy !== 0) {
            obj.balanceStrategy = balanceStrategyToJSON(message.balanceStrategy);
        }
        if (message.proxySetHeaders) {
            const entries = Object.entries(message.proxySetHeaders);
            if (entries.length > 0) {
                obj.proxySetHeaders = {};
                entries.forEach(([k, v]) => {
                    obj.proxySetHeaders[k] = v;
                });
            }
        }
        return obj;
    },
    create(base) {
        return Location.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseLocation();
        message.location = object.location ?? "";
        message.proxyRewrite = object.proxyRewrite ?? "";
        message.proxyPass = object.proxyPass?.map((e) => Upstream.fromPartial(e)) || [];
        message.balanceStrategy = object.balanceStrategy ?? 0;
        message.proxySetHeaders = Object.entries(object.proxySetHeaders ?? {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[key] = globalThis.String(value);
            }
            return acc;
        }, {});
        return message;
    },
};
function createBaseLocation_ProxySetHeadersEntry() {
    return { key: "", value: "" };
}
export const Location_ProxySetHeadersEntry = {
    encode(message, writer = new BinaryWriter()) {
        if (message.key !== "") {
            writer.uint32(10).string(message.key);
        }
        if (message.value !== "") {
            writer.uint32(18).string(message.value);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseLocation_ProxySetHeadersEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.key = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.value = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.String(object.key) : "",
            value: isSet(object.value) ? globalThis.String(object.value) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== "") {
            obj.key = message.key;
        }
        if (message.value !== "") {
            obj.value = message.value;
        }
        return obj;
    },
    create(base) {
        return Location_ProxySetHeadersEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseLocation_ProxySetHeadersEntry();
        message.key = object.key ?? "";
        message.value = object.value ?? "";
        return message;
    },
};
function createBaseSecurity() {
    return { rulesets: [], rules: [], limiters: [] };
}
export const Security = {
    encode(message, writer = new BinaryWriter()) {
        for (const v of message.rulesets) {
            Rulesets.encode(v, writer.uint32(10).fork()).join();
        }
        for (const v of message.rules) {
            RuleBased.encode(v, writer.uint32(18).fork()).join();
        }
        for (const v of message.limiters) {
            RateLimit.encode(v, writer.uint32(26).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseSecurity();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.rulesets.push(Rulesets.decode(reader, reader.uint32()));
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.rules.push(RuleBased.decode(reader, reader.uint32()));
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.limiters.push(RateLimit.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            rulesets: globalThis.Array.isArray(object?.rulesets) ? object.rulesets.map((e) => Rulesets.fromJSON(e)) : [],
            rules: globalThis.Array.isArray(object?.rules) ? object.rules.map((e) => RuleBased.fromJSON(e)) : [],
            limiters: globalThis.Array.isArray(object?.limiters)
                ? object.limiters.map((e) => RateLimit.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.rulesets?.length) {
            obj.rulesets = message.rulesets.map((e) => Rulesets.toJSON(e));
        }
        if (message.rules?.length) {
            obj.rules = message.rules.map((e) => RuleBased.toJSON(e));
        }
        if (message.limiters?.length) {
            obj.limiters = message.limiters.map((e) => RateLimit.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return Security.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseSecurity();
        message.rulesets = object.rulesets?.map((e) => Rulesets.fromPartial(e)) || [];
        message.rules = object.rules?.map((e) => RuleBased.fromPartial(e)) || [];
        message.limiters = object.limiters?.map((e) => RateLimit.fromPartial(e)) || [];
        return message;
    },
};
function createBaseRulesets() {
    return { enable: false };
}
export const Rulesets = {
    encode(message, writer = new BinaryWriter()) {
        if (message.enable !== false) {
            writer.uint32(8).bool(message.enable);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRulesets();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.enable = reader.bool();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { enable: isSet(object.enable) ? globalThis.Boolean(object.enable) : false };
    },
    toJSON(message) {
        const obj = {};
        if (message.enable !== false) {
            obj.enable = message.enable;
        }
        return obj;
    },
    create(base) {
        return Rulesets.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRulesets();
        message.enable = object.enable ?? false;
        return message;
    },
};
function createBaseRuleBased() {
    return { enable: false, ingress: undefined, ingressCompiled: undefined };
}
export const RuleBased = {
    encode(message, writer = new BinaryWriter()) {
        if (message.enable !== false) {
            writer.uint32(8).bool(message.enable);
        }
        if (message.ingress !== undefined) {
            RuleBasedLite.encode(message.ingress, writer.uint32(18).fork()).join();
        }
        if (message.ingressCompiled !== undefined) {
            RuleBased1.encode(message.ingressCompiled, writer.uint32(26).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRuleBased();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.enable = reader.bool();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.ingress = RuleBasedLite.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.ingressCompiled = RuleBased1.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            enable: isSet(object.enable) ? globalThis.Boolean(object.enable) : false,
            ingress: isSet(object.ingress) ? RuleBasedLite.fromJSON(object.ingress) : undefined,
            ingressCompiled: isSet(object.ingressCompiled) ? RuleBased1.fromJSON(object.ingressCompiled) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.enable !== false) {
            obj.enable = message.enable;
        }
        if (message.ingress !== undefined) {
            obj.ingress = RuleBasedLite.toJSON(message.ingress);
        }
        if (message.ingressCompiled !== undefined) {
            obj.ingressCompiled = RuleBased1.toJSON(message.ingressCompiled);
        }
        return obj;
    },
    create(base) {
        return RuleBased.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRuleBased();
        message.enable = object.enable ?? false;
        message.ingress = (object.ingress !== undefined && object.ingress !== null)
            ? RuleBasedLite.fromPartial(object.ingress)
            : undefined;
        message.ingressCompiled = (object.ingressCompiled !== undefined && object.ingressCompiled !== null)
            ? RuleBased1.fromPartial(object.ingressCompiled)
            : undefined;
        return message;
    },
};
function createBaseRateLimit() {
    return { enable: false, timeWindow: "", limit: 0, timeout: "" };
}
export const RateLimit = {
    encode(message, writer = new BinaryWriter()) {
        if (message.enable !== false) {
            writer.uint32(80).bool(message.enable);
        }
        if (message.timeWindow !== "") {
            writer.uint32(90).string(message.timeWindow);
        }
        if (message.limit !== 0) {
            writer.uint32(96).int64(message.limit);
        }
        if (message.timeout !== "") {
            writer.uint32(106).string(message.timeout);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseRateLimit();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 10: {
                    if (tag !== 80) {
                        break;
                    }
                    message.enable = reader.bool();
                    continue;
                }
                case 11: {
                    if (tag !== 90) {
                        break;
                    }
                    message.timeWindow = reader.string();
                    continue;
                }
                case 12: {
                    if (tag !== 96) {
                        break;
                    }
                    message.limit = longToNumber(reader.int64());
                    continue;
                }
                case 13: {
                    if (tag !== 106) {
                        break;
                    }
                    message.timeout = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            enable: isSet(object.enable) ? globalThis.Boolean(object.enable) : false,
            timeWindow: isSet(object.timeWindow) ? globalThis.String(object.timeWindow) : "",
            limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
            timeout: isSet(object.timeout) ? globalThis.String(object.timeout) : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.enable !== false) {
            obj.enable = message.enable;
        }
        if (message.timeWindow !== "") {
            obj.timeWindow = message.timeWindow;
        }
        if (message.limit !== 0) {
            obj.limit = Math.round(message.limit);
        }
        if (message.timeout !== "") {
            obj.timeout = message.timeout;
        }
        return obj;
    },
    create(base) {
        return RateLimit.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRateLimit();
        message.enable = object.enable ?? false;
        message.timeWindow = object.timeWindow ?? "";
        message.limit = object.limit ?? 0;
        message.timeout = object.timeout ?? "";
        return message;
    },
};
function createBaseController() {
    return { cdn: [] };
}
export const Controller = {
    encode(message, writer = new BinaryWriter()) {
        for (const v of message.cdn) {
            CDN.encode(v, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseController();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.cdn.push(CDN.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { cdn: globalThis.Array.isArray(object?.cdn) ? object.cdn.map((e) => CDN.fromJSON(e)) : [] };
    },
    toJSON(message) {
        const obj = {};
        if (message.cdn?.length) {
            obj.cdn = message.cdn.map((e) => CDN.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return Controller.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseController();
        message.cdn = object.cdn?.map((e) => CDN.fromPartial(e)) || [];
        return message;
    },
};
function createBasePersonal() {
    return {};
}
export const Personal = {
    encode(_, writer = new BinaryWriter()) {
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePersonal();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(_) {
        return {};
    },
    toJSON(_) {
        const obj = {};
        return obj;
    },
    create(base) {
        return Personal.fromPartial(base ?? {});
    },
    fromPartial(_) {
        const message = createBasePersonal();
        return message;
    },
};
function createBaseUpstream() {
    return { server: "", protocol: 0 };
}
export const Upstream = {
    encode(message, writer = new BinaryWriter()) {
        if (message.server !== "") {
            writer.uint32(10).string(message.server);
        }
        if (message.protocol !== 0) {
            writer.uint32(16).int32(message.protocol);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUpstream();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.server = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.protocol = reader.int32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            server: isSet(object.server) ? globalThis.String(object.server) : "",
            protocol: isSet(object.protocol) ? proxyProtocolFromJSON(object.protocol) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.server !== "") {
            obj.server = message.server;
        }
        if (message.protocol !== 0) {
            obj.protocol = proxyProtocolToJSON(message.protocol);
        }
        return obj;
    },
    create(base) {
        return Upstream.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUpstream();
        message.server = object.server ?? "";
        message.protocol = object.protocol ?? 0;
        return message;
    },
};
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isObject(value) {
    return typeof value === "object" && value !== null;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
