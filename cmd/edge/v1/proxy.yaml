routes:
  - match_prefix: /v1
    target: localhost:3000
    rewrite: /v1
    
  - match_prefix: /
    target: badcheese.com
    rewrite: /

  - match_prefix: /api
    target: localhost:8080
    rewrite: /
