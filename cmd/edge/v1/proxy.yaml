proxy:
  namespace: dev
  routes:
    - match_prefix: /
#      target: https://vneconomy.vn
      target: http://badcheese.com/

    # - match_prefix: /v1
    #   target: localhost:3000
    #   rewrite: /v1

    # - match_prefix: /api
    #   target: localhost:8080
