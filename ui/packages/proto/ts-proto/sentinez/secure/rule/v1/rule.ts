// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: sentinez/secure/rule/v1/rule.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sentinez.secure.rule.v1";

export enum RuleService {
  RULE_SERVICE_UNSPECIFIED = 0,
  RULE_SERVICE_CORE_RULESETS = 1,
  RULE_SERVICE_CUSTOM_RULES = 2,
  RULE_SERVICE_RATE_LIMIT = 3,
  UNRECOGNIZED = -1,
}

export function ruleServiceFromJSON(object: any): RuleService {
  switch (object) {
    case 0:
    case "RULE_SERVICE_UNSPECIFIED":
      return RuleService.RULE_SERVICE_UNSPECIFIED;
    case 1:
    case "RULE_SERVICE_CORE_RULESETS":
      return RuleService.RULE_SERVICE_CORE_RULESETS;
    case 2:
    case "RULE_SERVICE_CUSTOM_RULES":
      return RuleService.RULE_SERVICE_CUSTOM_RULES;
    case 3:
    case "RULE_SERVICE_RATE_LIMIT":
      return RuleService.RULE_SERVICE_RATE_LIMIT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RuleService.UNRECOGNIZED;
  }
}

export function ruleServiceToJSON(object: RuleService): string {
  switch (object) {
    case RuleService.RULE_SERVICE_UNSPECIFIED:
      return "RULE_SERVICE_UNSPECIFIED";
    case RuleService.RULE_SERVICE_CORE_RULESETS:
      return "RULE_SERVICE_CORE_RULESETS";
    case RuleService.RULE_SERVICE_CUSTOM_RULES:
      return "RULE_SERVICE_CUSTOM_RULES";
    case RuleService.RULE_SERVICE_RATE_LIMIT:
      return "RULE_SERVICE_RATE_LIMIT";
    case RuleService.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum RuleBehavior {
  RULE_BEHAVIOR_UNSPECIFIED = 0,
  RULE_BEHAVIOR_DENY = 1,
  UNRECOGNIZED = -1,
}

export function ruleBehaviorFromJSON(object: any): RuleBehavior {
  switch (object) {
    case 0:
    case "RULE_BEHAVIOR_UNSPECIFIED":
      return RuleBehavior.RULE_BEHAVIOR_UNSPECIFIED;
    case 1:
    case "RULE_BEHAVIOR_DENY":
      return RuleBehavior.RULE_BEHAVIOR_DENY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RuleBehavior.UNRECOGNIZED;
  }
}

export function ruleBehaviorToJSON(object: RuleBehavior): string {
  switch (object) {
    case RuleBehavior.RULE_BEHAVIOR_UNSPECIFIED:
      return "RULE_BEHAVIOR_UNSPECIFIED";
    case RuleBehavior.RULE_BEHAVIOR_DENY:
      return "RULE_BEHAVIOR_DENY";
    case RuleBehavior.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface CoreRulesets {
  name: string;
  rules: CoreRule[];
  version: string;
}

export interface CoreRule {
  actions?: RuleAction | undefined;
  configuration: string;
  level: string;
}

export interface RuleAction {
  statement: string;
  children?: RuleAction | undefined;
  fields?: RuleActionField | undefined;
}

export interface RuleActionField {
  id: string[];
  logdata: string[];
  msg: string[];
  phase: string[];
  setvar: string[];
  severity: string[];
  t: string[];
  ver: string[];
  tag: string[];
}

function createBaseCoreRulesets(): CoreRulesets {
  return { name: "", rules: [], version: "" };
}

export const CoreRulesets: MessageFns<CoreRulesets> = {
  encode(message: CoreRulesets, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.rules) {
      CoreRule.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CoreRulesets {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCoreRulesets();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.rules.push(CoreRule.decode(reader, reader.uint32()));
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.version = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CoreRulesets {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      rules: globalThis.Array.isArray(object?.rules) ? object.rules.map((e: any) => CoreRule.fromJSON(e)) : [],
      version: isSet(object.version) ? globalThis.String(object.version) : "",
    };
  },

  toJSON(message: CoreRulesets): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.rules?.length) {
      obj.rules = message.rules.map((e) => CoreRule.toJSON(e));
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CoreRulesets>, I>>(base?: I): CoreRulesets {
    return CoreRulesets.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CoreRulesets>, I>>(object: I): CoreRulesets {
    const message = createBaseCoreRulesets();
    message.name = object.name ?? "";
    message.rules = object.rules?.map((e) => CoreRule.fromPartial(e)) || [];
    message.version = object.version ?? "";
    return message;
  },
};

function createBaseCoreRule(): CoreRule {
  return { actions: undefined, configuration: "", level: "" };
}

export const CoreRule: MessageFns<CoreRule> = {
  encode(message: CoreRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.actions !== undefined) {
      RuleAction.encode(message.actions, writer.uint32(10).fork()).join();
    }
    if (message.configuration !== "") {
      writer.uint32(18).string(message.configuration);
    }
    if (message.level !== "") {
      writer.uint32(26).string(message.level);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CoreRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCoreRule();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.actions = RuleAction.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.configuration = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.level = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CoreRule {
    return {
      actions: isSet(object.actions) ? RuleAction.fromJSON(object.actions) : undefined,
      configuration: isSet(object.configuration) ? globalThis.String(object.configuration) : "",
      level: isSet(object.level) ? globalThis.String(object.level) : "",
    };
  },

  toJSON(message: CoreRule): unknown {
    const obj: any = {};
    if (message.actions !== undefined) {
      obj.actions = RuleAction.toJSON(message.actions);
    }
    if (message.configuration !== "") {
      obj.configuration = message.configuration;
    }
    if (message.level !== "") {
      obj.level = message.level;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CoreRule>, I>>(base?: I): CoreRule {
    return CoreRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CoreRule>, I>>(object: I): CoreRule {
    const message = createBaseCoreRule();
    message.actions = (object.actions !== undefined && object.actions !== null)
      ? RuleAction.fromPartial(object.actions)
      : undefined;
    message.configuration = object.configuration ?? "";
    message.level = object.level ?? "";
    return message;
  },
};

function createBaseRuleAction(): RuleAction {
  return { statement: "", children: undefined, fields: undefined };
}

export const RuleAction: MessageFns<RuleAction> = {
  encode(message: RuleAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.statement !== "") {
      writer.uint32(10).string(message.statement);
    }
    if (message.children !== undefined) {
      RuleAction.encode(message.children, writer.uint32(18).fork()).join();
    }
    if (message.fields !== undefined) {
      RuleActionField.encode(message.fields, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RuleAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRuleAction();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.statement = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.children = RuleAction.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.fields = RuleActionField.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RuleAction {
    return {
      statement: isSet(object.statement) ? globalThis.String(object.statement) : "",
      children: isSet(object.children) ? RuleAction.fromJSON(object.children) : undefined,
      fields: isSet(object.fields) ? RuleActionField.fromJSON(object.fields) : undefined,
    };
  },

  toJSON(message: RuleAction): unknown {
    const obj: any = {};
    if (message.statement !== "") {
      obj.statement = message.statement;
    }
    if (message.children !== undefined) {
      obj.children = RuleAction.toJSON(message.children);
    }
    if (message.fields !== undefined) {
      obj.fields = RuleActionField.toJSON(message.fields);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RuleAction>, I>>(base?: I): RuleAction {
    return RuleAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RuleAction>, I>>(object: I): RuleAction {
    const message = createBaseRuleAction();
    message.statement = object.statement ?? "";
    message.children = (object.children !== undefined && object.children !== null)
      ? RuleAction.fromPartial(object.children)
      : undefined;
    message.fields = (object.fields !== undefined && object.fields !== null)
      ? RuleActionField.fromPartial(object.fields)
      : undefined;
    return message;
  },
};

function createBaseRuleActionField(): RuleActionField {
  return { id: [], logdata: [], msg: [], phase: [], setvar: [], severity: [], t: [], ver: [], tag: [] };
}

export const RuleActionField: MessageFns<RuleActionField> = {
  encode(message: RuleActionField, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.id) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.logdata) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.msg) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.phase) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.setvar) {
      writer.uint32(42).string(v!);
    }
    for (const v of message.severity) {
      writer.uint32(50).string(v!);
    }
    for (const v of message.t) {
      writer.uint32(58).string(v!);
    }
    for (const v of message.ver) {
      writer.uint32(66).string(v!);
    }
    for (const v of message.tag) {
      writer.uint32(74).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RuleActionField {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRuleActionField();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.id.push(reader.string());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.logdata.push(reader.string());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.msg.push(reader.string());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.phase.push(reader.string());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.setvar.push(reader.string());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.severity.push(reader.string());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.t.push(reader.string());
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.ver.push(reader.string());
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.tag.push(reader.string());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RuleActionField {
    return {
      id: globalThis.Array.isArray(object?.id) ? object.id.map((e: any) => globalThis.String(e)) : [],
      logdata: globalThis.Array.isArray(object?.logdata) ? object.logdata.map((e: any) => globalThis.String(e)) : [],
      msg: globalThis.Array.isArray(object?.msg) ? object.msg.map((e: any) => globalThis.String(e)) : [],
      phase: globalThis.Array.isArray(object?.phase) ? object.phase.map((e: any) => globalThis.String(e)) : [],
      setvar: globalThis.Array.isArray(object?.setvar) ? object.setvar.map((e: any) => globalThis.String(e)) : [],
      severity: globalThis.Array.isArray(object?.severity) ? object.severity.map((e: any) => globalThis.String(e)) : [],
      t: globalThis.Array.isArray(object?.t) ? object.t.map((e: any) => globalThis.String(e)) : [],
      ver: globalThis.Array.isArray(object?.ver) ? object.ver.map((e: any) => globalThis.String(e)) : [],
      tag: globalThis.Array.isArray(object?.tag) ? object.tag.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: RuleActionField): unknown {
    const obj: any = {};
    if (message.id?.length) {
      obj.id = message.id;
    }
    if (message.logdata?.length) {
      obj.logdata = message.logdata;
    }
    if (message.msg?.length) {
      obj.msg = message.msg;
    }
    if (message.phase?.length) {
      obj.phase = message.phase;
    }
    if (message.setvar?.length) {
      obj.setvar = message.setvar;
    }
    if (message.severity?.length) {
      obj.severity = message.severity;
    }
    if (message.t?.length) {
      obj.t = message.t;
    }
    if (message.ver?.length) {
      obj.ver = message.ver;
    }
    if (message.tag?.length) {
      obj.tag = message.tag;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RuleActionField>, I>>(base?: I): RuleActionField {
    return RuleActionField.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RuleActionField>, I>>(object: I): RuleActionField {
    const message = createBaseRuleActionField();
    message.id = object.id?.map((e) => e) || [];
    message.logdata = object.logdata?.map((e) => e) || [];
    message.msg = object.msg?.map((e) => e) || [];
    message.phase = object.phase?.map((e) => e) || [];
    message.setvar = object.setvar?.map((e) => e) || [];
    message.severity = object.severity?.map((e) => e) || [];
    message.t = object.t?.map((e) => e) || [];
    message.ver = object.ver?.map((e) => e) || [];
    message.tag = object.tag?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
