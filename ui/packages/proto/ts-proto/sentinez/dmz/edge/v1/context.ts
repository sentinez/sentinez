// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: sentinez/dmz/edge/v1/context.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Request } from "../../../network/http/v1/http";
import { Transport } from "../../../network/v1/conn";
import { Metadata } from "./setting";

export const protobufPackage = "sentinez.dmz.edge.v1";

/** Context helps the edge identify which user is connected */
export interface Context {
  metadata?: Metadata | undefined;
  transport?: Transport | undefined;
  request?: Request | undefined;
  x?: ContextExtra | undefined;
}

export interface ContextExtra {
  namespace: string;
  ruleBasedMatchedIds: string[];
  ruleBasedMatchedNames: string[];
  rulesetsMatchedIds: string[];
  rulesetsMatchedNames: string[];
  rulesetsMatchedSeverity: string[];
}

function createBaseContext(): Context {
  return { metadata: undefined, transport: undefined, request: undefined, x: undefined };
}

export const Context: MessageFns<Context> = {
  encode(message: Context, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(10).fork()).join();
    }
    if (message.transport !== undefined) {
      Transport.encode(message.transport, writer.uint32(18).fork()).join();
    }
    if (message.request !== undefined) {
      Request.encode(message.request, writer.uint32(26).fork()).join();
    }
    if (message.x !== undefined) {
      ContextExtra.encode(message.x, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Context {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseContext();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.metadata = Metadata.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.transport = Transport.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.request = Request.decode(reader, reader.uint32());
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.x = ContextExtra.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Context {
    return {
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
      transport: isSet(object.transport) ? Transport.fromJSON(object.transport) : undefined,
      request: isSet(object.request) ? Request.fromJSON(object.request) : undefined,
      x: isSet(object.x) ? ContextExtra.fromJSON(object.x) : undefined,
    };
  },

  toJSON(message: Context): unknown {
    const obj: any = {};
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    if (message.transport !== undefined) {
      obj.transport = Transport.toJSON(message.transport);
    }
    if (message.request !== undefined) {
      obj.request = Request.toJSON(message.request);
    }
    if (message.x !== undefined) {
      obj.x = ContextExtra.toJSON(message.x);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Context>, I>>(base?: I): Context {
    return Context.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Context>, I>>(object: I): Context {
    const message = createBaseContext();
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    message.transport = (object.transport !== undefined && object.transport !== null)
      ? Transport.fromPartial(object.transport)
      : undefined;
    message.request = (object.request !== undefined && object.request !== null)
      ? Request.fromPartial(object.request)
      : undefined;
    message.x = (object.x !== undefined && object.x !== null) ? ContextExtra.fromPartial(object.x) : undefined;
    return message;
  },
};

function createBaseContextExtra(): ContextExtra {
  return {
    namespace: "",
    ruleBasedMatchedIds: [],
    ruleBasedMatchedNames: [],
    rulesetsMatchedIds: [],
    rulesetsMatchedNames: [],
    rulesetsMatchedSeverity: [],
  };
}

export const ContextExtra: MessageFns<ContextExtra> = {
  encode(message: ContextExtra, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.namespace !== "") {
      writer.uint32(10).string(message.namespace);
    }
    for (const v of message.ruleBasedMatchedIds) {
      writer.uint32(82).string(v!);
    }
    for (const v of message.ruleBasedMatchedNames) {
      writer.uint32(90).string(v!);
    }
    for (const v of message.rulesetsMatchedIds) {
      writer.uint32(98).string(v!);
    }
    for (const v of message.rulesetsMatchedNames) {
      writer.uint32(106).string(v!);
    }
    for (const v of message.rulesetsMatchedSeverity) {
      writer.uint32(114).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContextExtra {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseContextExtra();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.namespace = reader.string();
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.ruleBasedMatchedIds.push(reader.string());
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.ruleBasedMatchedNames.push(reader.string());
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.rulesetsMatchedIds.push(reader.string());
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.rulesetsMatchedNames.push(reader.string());
            continue;
          }
          case 14: {
            if (tag !== 114) {
              break;
            }

            message.rulesetsMatchedSeverity.push(reader.string());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ContextExtra {
    return {
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      ruleBasedMatchedIds: globalThis.Array.isArray(object?.ruleBasedMatchedIds)
        ? object.ruleBasedMatchedIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.rule_based_matched_ids)
        ? object.rule_based_matched_ids.map((e: any) => globalThis.String(e))
        : [],
      ruleBasedMatchedNames: globalThis.Array.isArray(object?.ruleBasedMatchedNames)
        ? object.ruleBasedMatchedNames.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.rule_based_matched_names)
        ? object.rule_based_matched_names.map((e: any) => globalThis.String(e))
        : [],
      rulesetsMatchedIds: globalThis.Array.isArray(object?.rulesetsMatchedIds)
        ? object.rulesetsMatchedIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.rulesets_matched_ids)
        ? object.rulesets_matched_ids.map((e: any) => globalThis.String(e))
        : [],
      rulesetsMatchedNames: globalThis.Array.isArray(object?.rulesetsMatchedNames)
        ? object.rulesetsMatchedNames.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.rulesets_matched_names)
        ? object.rulesets_matched_names.map((e: any) => globalThis.String(e))
        : [],
      rulesetsMatchedSeverity: globalThis.Array.isArray(object?.rulesetsMatchedSeverity)
        ? object.rulesetsMatchedSeverity.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.rulesets_matched_severity)
        ? object.rulesets_matched_severity.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ContextExtra): unknown {
    const obj: any = {};
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.ruleBasedMatchedIds?.length) {
      obj.ruleBasedMatchedIds = message.ruleBasedMatchedIds;
    }
    if (message.ruleBasedMatchedNames?.length) {
      obj.ruleBasedMatchedNames = message.ruleBasedMatchedNames;
    }
    if (message.rulesetsMatchedIds?.length) {
      obj.rulesetsMatchedIds = message.rulesetsMatchedIds;
    }
    if (message.rulesetsMatchedNames?.length) {
      obj.rulesetsMatchedNames = message.rulesetsMatchedNames;
    }
    if (message.rulesetsMatchedSeverity?.length) {
      obj.rulesetsMatchedSeverity = message.rulesetsMatchedSeverity;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ContextExtra>, I>>(base?: I): ContextExtra {
    return ContextExtra.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContextExtra>, I>>(object: I): ContextExtra {
    const message = createBaseContextExtra();
    message.namespace = object.namespace ?? "";
    message.ruleBasedMatchedIds = object.ruleBasedMatchedIds?.map((e) => e) || [];
    message.ruleBasedMatchedNames = object.ruleBasedMatchedNames?.map((e) => e) || [];
    message.rulesetsMatchedIds = object.rulesetsMatchedIds?.map((e) => e) || [];
    message.rulesetsMatchedNames = object.rulesetsMatchedNames?.map((e) => e) || [];
    message.rulesetsMatchedSeverity = object.rulesetsMatchedSeverity?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
